const jsonHeaders = () => { const headers = new Headers({ 'Content-Type': 'application/json' }); const token = localStorage.getItem('freshcart_token'); if (token) headers.set('Authorization', `Bearer ${token}`); return headers; };
export async function shopkeeperProducts<T=unknown>() { const r=await fetch('/api/shopkeeper/products',{headers:jsonHeaders()}); if(!r.ok) throw new Error((await r.json().catch(()=>null))?.error||`Request failed (${r.status})`); return r.json() as Promise<T>; }
export async function shopkeeperStock<T=unknown>(id:string, stock:number) { const r=await fetch(`/api/shopkeeper/products/${id}/stock`,{method:'PATCH',headers:jsonHeaders(),body:JSON.stringify({stock})}); if(!r.ok) throw new Error((await r.json().catch(()=>null))?.error||`Request failed (${r.status})`); return r.json() as Promise<T>; }
export async function submitShopkeeperCsv<T=unknown>(fileName:string,csv:string) { const r=await fetch('/api/onboarding/sales-imports',{method:'POST',headers:jsonHeaders(),body:JSON.stringify({fileName,csv})}); if(!r.ok) throw new Error((await r.json().catch(()=>null))?.error||`Request failed (${r.status})`); return r.json() as Promise<T>; }
export async function adminSalesImports<T=unknown>() { const r=await fetch('/api/admin/sales-imports',{headers:jsonHeaders()}); if(!r.ok) throw new Error((await r.json().catch(()=>null))?.error||`Request failed (${r.status})`); return r.json() as Promise<T>; }
export async function adminApproveCsv<T=unknown>(referenceId:string) { const r=await fetch(`/api/admin/sales-imports/${encodeURIComponent(referenceId)}/approve`,{method:'POST',headers:jsonHeaders()}); if(!r.ok) throw new Error((await r.json().catch(()=>null))?.error||`Request failed (${r.status})`); return r.json() as Promise<T>; }
export async function adminRejectCsv<T=unknown>(referenceId:string, reason:string) { const r=await fetch(`/api/admin/sales-imports/${encodeURIComponent(referenceId)}/reject`,{method:'POST',headers:jsonHeaders(),body:JSON.stringify({reason})}); if(!r.ok) throw new Error((await r.json().catch(()=>null))?.error||`Request failed (${r.status})`); return r.json() as Promise<T>; }
